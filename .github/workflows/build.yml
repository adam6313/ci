name: continuous integration

on: push

jobs:
  test:
    #strategy:
    #matrix:
    #go-version: [1.14.x]
    runs-on: self-hosted

    services:
      mongo:
        image: mongo:latest
        ports:
          - 27017:27017

    steps:
      - name: Build test environment
        uses: actions/checkout@v2
        run: |
          docker run --rm -it -v ${PWD}:/app golang
          cd /app
          go test ./...
    #steps:
    #- name: Install Go
    #uses: actions/setup-go@v2
    #with:
    #go-version: ${{ matrix.go-version }}
    #- name: Checkout code
    #uses: actions/checkout@v2
    #- name: Test
    #run: |
    #go test ./...
