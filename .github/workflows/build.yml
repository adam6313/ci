name: ci

on:
  push:
    tags:
      - "v*.*.*.beta"
      - "v*.*.*.beta*"

  release:
    types:
      - created

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2

      - name: echo
        run: |
          echo ${{ github.event }}



